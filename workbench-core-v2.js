/* 晓的工作台 V3：统一数据、领域与计划关系核心 */
(function(){
const DOMAIN={civil:{name:'公务员',areas:['civil','xingce','shenlun','current','errors','mock'],words:/公务员|公考|行测|申论|时政/},teacher:{name:'教师编',areas:['teacher'],words:/教师编|教师招聘|教综/},psy:{name:'心理学',areas:['psy'],words:/心理学/},health:{name:'健康',areas:['health'],words:/健康|运动|睡眠|饮水/},life:{name:'生活',areas:['life'],words:/生活|家务|整理/}};const KNOWN=new Set(Object.values(DOMAIN).flatMap(x=>x.areas));
function today(){return new Date().toISOString().slice(0,10)}function sameId(a,b){return a!=null&&b!=null&&String(a)===String(b)}function normalizeTask(t){if(!t)return t;if(!t.date)t.date=today();if(!t.priority)t.priority='medium';if(t.planId===undefined)t.planId=null;if(t.planId==='')t.planId=null;if(t.note===undefined)t.note='';return t}function normalizePlan(p){if(!p)return p;let par=p.parentId??p.parent??null;if(par==='')par=null;p.parentId=par;p.parent=par;if(!p.level)p.level='month';if(p.domain===undefined)p.domain=null;return p}function parentId(p){return normalizePlan(p)?.parentId??null}function children(id){return (window.data?.plans||[]).map(normalizePlan).filter(p=>sameId(parentId(p),id))}function descendants(id){let out=[],seen=new Set([String(id)]),q=[id];while(q.length){let cur=q.shift();children(cur).forEach(p=>{let k=String(p.id);if(seen.has(k))return;seen.add(k);out.push(p);q.push(p.id)})}return out}function domainOf(t){normalizeTask(t);if(['civil','teacher','psy','health','life'].includes(t.domain))return t.domain;let a=String(t.area||'').toLowerCase();for(const [k,c] of Object.entries(DOMAIN))if(c.areas.includes(a))return k;if(a&&KNOWN.has(a))return null;let text=String(t.text||'');for(const [k,c] of Object.entries(DOMAIN))if(c.words.test(text))return k;return null}function tasks(domain,opts={}){let a=(window.data?.tasks||[]).map(normalizeTask);if(domain)a=a.filter(t=>domainOf(t)===domain);if(opts.date)a=a.filter(t=>t.date===opts.date);if(opts.done!==undefined)a=a.filter(t=>!!t.done===opts.done);if(opts.planId!=null)a=a.filter(t=>sameId(t.planId,opts.planId));return a}function stats(domain,opts={}){let a=tasks(domain,opts),done=a.filter(t=>t.done).length;return{total:a.length,done,pending:a.length-done,pct:a.length?Math.round(done/a.length*100):0}}function plans(domain){let all=(window.data?.plans||[]).map(normalizePlan);if(!domain)return all;if(window.DomainScope)return DomainScope.plans(domain);return all.filter(p=>p.domain===domain||tasks(domain).some(t=>sameId(t.planId,p.id)))}function migrate(){if(!window.data)return;let changed=false;(data.plans||[]).forEach(p=>{let before=JSON.stringify(p);normalizePlan(p);if(JSON.stringify(p)!==before)changed=true});(data.tasks||[]).forEach(t=>{let before=JSON.stringify(t);normalizeTask(t);let d=domainOf(t);if(d&&!t.domain)t.domain=d;if(t.planId!=null&&!data.plans?.some(p=>sameId(p.id,t.planId)))t.planId=null;if(JSON.stringify(t)!==before)changed=true});if(changed&&typeof save==='function')save()}
window.WorkbenchCore={DOMAIN,today,sameId,normalizeTask,normalizePlan,parentId,children,descendants,domainOf,tasks,stats,plans,migrate};setTimeout(migrate,300);
})();