/* 晓的工作台：模考自动诊断 v1 */
(function(){
const KEY='xiao_civil_service_v1';function read(){try{return Object.assign({mocks:[],errors:[],sessions:[],shenlun:[]},JSON.parse(localStorage.getItem(KEY))||{})}catch{return{mocks:[],errors:[],sessions:[],shenlun:[]}}}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function goal(){return typeof civilGoalSnapshot==='function'?civilGoalSnapshot():{configured:false,xGap:0,sGap:0,phase:'未设置'}}function trend(c,m){let i=c.mocks.findIndex(x=>x.id===m.id),prev=i>=0?c.mocks[i+1]:null;return prev?{total:(+m.xingce)+(+m.shenlun)-(+prev.xingce)-(+prev.shenlun),x:(+m.xingce)-(+prev.xingce),s:(+m.shenlun)-(+prev.shenlun)}:null}function plan(c,m){let g=goal(),t=trend(c,m),out=[];if(m.weak)out.push({area:'xingce',text:`${m.weak}专项训练 30 题`,why:`本次模考标记为最薄弱模块`});if(g.configured&&g.xGap>=8)out.push({area:'xingce',text:'行测限时训练 1 组',why:`行测距目标还有 ${g.xGap} 分`});if(g.configured&&g.sGap>=8)out.push({area:'shenlun',text:'申论完整作答 1 题',why:`申论距目标还有 ${g.sGap} 分`});let pending=c.errors.filter(x=>!x.reviewed).length;if(pending)out.push({area:'errors',text:`错题复盘 ${Math.min(pending,10)} 条`,why:`当前有 ${pending} 条待复盘`});if(t&&t.total<0)out.push({area:'errors',text:'复盘本次模考失分点',why:`总分较上次下降 ${Math.abs(t.total)} 分`});return out.slice(0,4)}window.openCivilMockReview=function(id){let c=read(),m=c.mocks.find(x=>x.id===id)||c.mocks[0];if(!m)return;let g=goal(),t=trend(c,m),p=plan(c,m),total=(+m.xingce)+(+m.shenlun),main=(+m.xingce)<(+m.shenlun)?'行测':'申论';show(`<h3>📊 模考诊断 · ${esc(m.name)}</h3><div class="summary"><div class="card"><div class="num">${total}</div><small>本次总分</small></div><div class="card"><div class="num">${m.xingce}</div><small>行测</small></div><div class="card"><div class="num">${m.shenlun}</div><small>申论</small></div></div>${t?`<div class="node"><b>与上次相比</b><div style="margin-top:6px">总分 ${t.total>=0?'+':''}${t.total} · 行测 ${t.x>=0?'+':''}${t.x} · 申论 ${t.s>=0?'+':''}${t.s}</div></div>`:''}<div class="node"><b>诊断结论</b><div style="line-height:1.7;margin-top:6px">${m.weak?`行测重点关注：${esc(m.weak)}。<br>`:''}${g.configured?`当前处于 ${g.phase}，行测距目标 ${g.xGap} 分，申论距目标 ${g.sGap} 分。`:`当前${main}分数相对更低，可优先检查这一科的提分空间。`}</div></div><div class="node"><b>下一阶段提分方案</b>${p.length?p.map((x,i)=>`<div class="task"><div class="txt"><b>${i+1}. ${esc(x.text)}</b><div class="muted">${esc(x.why)}</div></div><button class="mini" onclick="addMockReviewTask('${esc(x.text)}','${x.area}')">加入</button></div>`).join(''):'<div class="muted">当前数据较少，继续记录专项训练和错题后会生成更具体的方案。</div>'}</div>${p.length?`<button class="btn" style="width:100%" onclick="addAllMockReviewTasks(${m.id})">全部加入今日任务</button>`:''}<button class="mini" style="width:100%;margin-top:8px" onclick="openCivilHub()">返回备考中心</button>`)};window.addMockReviewTask=function(text,area){if(!window.data?.tasks)return;if(!data.tasks.some(t=>!t.done&&t.text===text))data.tasks.push({id:Date.now(),area,text,done:false,mockReview:true});if(typeof save==='function')save()};window.addAllMockReviewTasks=function(id){let c=read(),m=c.mocks.find(x=>x.id===id);if(!m||!window.data?.tasks)return;plan(c,m).forEach((x,i)=>{if(!data.tasks.some(t=>!t.done&&t.text===x.text))data.tasks.push({id:Date.now()+i,area:x.area,text:x.text,done:false,mockReview:true})});if(typeof save==='function')save()};
const oldSave=window.saveCivilMock;window.saveCivilMock=function(){oldSave();setTimeout(()=>{let c=read();if(c.mocks[0])openCivilMockReview(c.mocks[0].id)},60)};const oldHub=window.openCivilHub;window.openCivilHub=function(){oldHub();setTimeout(()=>{let c=read(),modal=document.getElementById('modalContent');if(c.mocks[0]&&modal&&!document.getElementById('latestMockReviewBtn')){let b=document.createElement('button');b.id='latestMockReviewBtn';b.className='mini';b.style='width:100%;margin-top:8px';b.textContent='📊 查看最近模考诊断';b.onclick=()=>openCivilMockReview(c.mocks[0].id);modal.appendChild(b)}},0)};
})();